'use client';import Header from '@/components/Header';import CarCard from '@/components/CarCard';import {getVehicles} from '@/lib/store';import {useEffect,useMemo,useState} from 'react';import {Vehicle} from '@/lib/types';
export default function Cars(){const [cars,setCars]=useState<Vehicle[]>([]),[make,setMake]=useState(''),[fuel,setFuel]=useState(''),[status,setStatus]=useState('');useEffect(()=>setCars(getVehicles().filter(v=>v.published&&v.status!=='Draft')),[]);const filtered=useMemo(()=>cars.filter(v=>(!make||v.manufacturer===make)&&(!fuel||v.fuel===fuel)&&(!status||v.status===status)),[cars,make,fuel,status]);return <><Header/><main className="shell section"><div className="label">Inventory</div><h1 style={{fontSize:52,marginTop:8}}>Cars</h1><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(160px,1fr))',gap:10,margin:'24px 0 32px'}}><select className="select" value={make} onChange={e=>setMake(e.target.value)}><option value="">All manufacturers</option>{[...new Set(cars.map(x=>x.manufacturer))].map(x=><option key={x}>{x}</option>)}</select><select className="select" value={fuel} onChange={e=>setFuel(e.target.value)}><option value="">All fuel types</option>{[...new Set(cars.map(x=>x.fuel))].map(x=><option key={x}>{x}</option>)}</select><select className="select" value={status} onChange={e=>setStatus(e.target.value)}><option value="">All statuses</option><option>Available</option><option>Reserved</option><option>Sold</option></select></div><div className="grid-cards">{filtered.map(v=><CarCard key={v.id} v={v}/>)}</div></main></>}
