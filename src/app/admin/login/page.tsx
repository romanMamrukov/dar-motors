'use client';
function withBasePath(path:string){const base=process.env.NEXT_PUBLIC_BASE_PATH||'';return `${base}${path}`}
export default function Login(){function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);if(f.get('email')==='demo@darmotors.local'&&f.get('password')==='demo1234'){sessionStorage.setItem('dar_demo_auth','1');window.location.assign(withBasePath('/admin/'))}else alert('Use the demo credentials shown below.')}return <main className="shell section" style={{maxWidth:520}}><div className="label">D.A.R. Motors</div><h1>Admin login</h1><p>Demo credentials: <b>demo@darmotors.local</b> / <b>demo1234</b></p><form onSubmit={go} style={{display:'grid',gap:12}}><input className="input" name="email" type="email" placeholder="Email" required/><input className="input" name="password" type="password" placeholder="Password" required/><button className="btn brand-btn">Log in</button></form><p style={{color:'#6b7280',fontSize:13}}>Demo authentication is browser-session only. Production must use Supabase Auth + RLS.</p></main>}
