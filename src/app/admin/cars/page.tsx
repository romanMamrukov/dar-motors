'use client';import Link from 'next/link';import {useEffect,useState} from 'react';import {getVehicles,saveVehicles} from '@/lib/store';import {Vehicle,VehicleStatus} from '@/lib/types';import {eur} from '@/lib/format';export default function AdminCars(){const[v,setV]=useState<Vehicle[]>([]);useEffect(()=>setV(getVehicles()),[]);function status(id:string,s:VehicleStatus){const n=v.map(x=>x.id===id?{...x,status:s,published:s!=='Draft'}:x);setV(n);saveVehicles(n)}function del(id:string){if(!confirm('Delete this vehicle?'))return;const n=v.filter(x=>x.id!==id);setV(n);saveVehicles(n)}return <><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><div className="label">Inventory</div><h1>Vehicles</h1></div><Link className="btn" href="/admin/cars/new">Add vehicle</Link></div><div style={{overflowX:'auto'}}><table className="table"><thead><tr><th>Vehicle</th><th>Price</th><th>Status</th><th>Actions</th></tr></thead><tbody>{v.map(x=><tr key={x.id}><td><b>{x.manufacturer} {x.model}</b><br/><span className="label">{x.year}</span></td><td>{eur(x.price)}</td><td><span className={`status ${x.status}`}>{x.status}</span></td><td><div style={{display:'flex',gap:8,flexWrap:'wrap'}}><Link href={`/admin/car?id=${encodeURIComponent(x.id)}`} className="btn secondary">Edit</Link><select className="select" style={{width:130}} value={x.status} onChange={e=>status(x.id,e.target.value as VehicleStatus)}><option>Available</option><option>Reserved</option><option>Sold</option><option>Draft</option></select><button className="btn danger" onClick={()=>del(x.id)}>Delete</button></div></td></tr>)}</tbody></table></div></>}
