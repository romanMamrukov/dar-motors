import {describe,it,expect} from 'vitest';import {leadSchema,vehicleSchema} from './validation';describe('validation',()=>{it('accepts a valid lead',()=>expect(leadSchema.safeParse({customer:'Anna',phone:'20000000',email:'',message:'Interested'}).success).toBe(true));it('rejects invalid vehicle',()=>expect(vehicleSchema.safeParse({manufacturer:'',model:'A',year:1900,price:-1,mileage:1}).success).toBe(false))});