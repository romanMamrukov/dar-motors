import {z} from 'zod'; export const leadSchema=z.object({customer:z.string().min(2),phone:z.string().min(6),email:z.string().email().optional().or(z.literal('')),message:z.string().min(3)}); export const vehicleSchema=z.object({manufacturer:z.string().min(1),model:z.string().min(1),year:z.number().min(1950).max(2030),price:z.number().nonnegative(),mileage:z.number().nonnegative()});